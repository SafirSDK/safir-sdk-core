with "externally_built_library";
with "dots_generated_ada";
with "dose_dll";

project Dose_Ada is

    for Source_Dirs use (External ("SAFIR_SDK") & "/ada/dose_ada/interface");

    for Library_Dir use Externally_Built_Library'Library_Dir;

    for Library_Name use "dose_ada";

    for Library_ALI_Dir use External ("SAFIR_SDK") & "/ada/dose_ada/ali";

    for Library_Kind use "dynamic";

    for Externally_Built use "true";

end Dose_Ada;
