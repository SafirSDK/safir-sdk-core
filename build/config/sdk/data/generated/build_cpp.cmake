cmake_minimum_required(VERSION 2.8)

PROJECT (dots_generated-${SAFIR_PROJECT_NAME} CXX)

INCLUDE_DIRECTORIES(include)

INCLUDE($ENV{SAFIR_SDK}/data/build/safir_sdk_core_config.cmake)

FILE(GLOB_RECURSE headers *.h)
FILE(GLOB sources *.cpp)

ADD_LIBRARY(${PROJECT_NAME} SHARED ${sources} ${headers})

#TODO
#ADD_DEFINITIONS(-DDOTS_EXPORTS)

#FIND_PACKAGE(Boost COMPONENTS thread system)

#message("DEPENDENCIES = ${DEPENDENCIES}")

if (NOT MSVC)
  #TARGET_LINK_LIBRARIES(dots_generated-${SAFIR_PROJECT_NAME}-cpp ${Boost_LIBRARIES})
  FOREACH (DEP ${DEPENDENCIES})
    TARGET_LINK_LIBRARIES(dots_generated-${SAFIR_PROJECT_NAME} dots_generated-${DEP}-cpp)
  ENDFOREACH()
endif()

INSTALL(TARGETS ${PROJECT_NAME}
  RUNTIME DESTINATION ${SAFIR_RUNTIME}/bin
  LIBRARY DESTINATION ${SAFIR_RUNTIME}/lib
  ARCHIVE DESTINATION ${SAFIR_SDK}/lib)

INSTALL(DIRECTORY include DESTINATION ${SAFIR_SDK}
  PATTERN ".svn" EXCLUDE
  PATTERN "*~" EXCLUDE)
