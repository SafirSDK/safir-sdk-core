#IMPORTANT: THIS FILE NEEDS TO BE UPDATED FOR EVERY RELEASE
#Remember: any updates that change APIs should update MAJOR, since
#that is used for SOVERSION.

MAJOR=7
MINOR=4
PATCH=3
SUFFIX=

#Set the SUFFIX to empty string for stable releases (but keep the equal sign).
#If the SUFFIX is used for alpha or beta tags, start it with a dash (-),
#so for example it could be set like this: SUFFIX=-alpha1
#The dash is the canonical form, used everywhere: git tags, the Windows
#installer, NuGet and the GitHub release asset names. The Debian package build
#is the single place that translates the dash to a tilde (~), because debian
#versioning needs a tilde for pre-release ordering (7.4.3~alpha1 sorts before
#7.4.3). See DebianPackager.build in build/safir_build_common.py, and keep
#build/packaging/debian/changelog written with the tilde form to match.

#Note: Keep to 7-bit ascii in this file, or everything will blow up...


#Note: When you change this you also have to change multiple installcligac
#      files inside build/packaging/debian to reflect the updated Policy file
#      names.
