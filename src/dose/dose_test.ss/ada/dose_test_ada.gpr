with "safir_defaults";
with "dots_generated_ada";
with "dots_ada";
with "dose_ada";
with "swre_interface_ada";

project Dose_Test_Ada is

   for Source_Dirs use (".");
   for Exec_Dir use "obj";
   for Object_Dir use "obj";

   for Main use ("dose_test_ada.adb");

   package Builder is
      for Default_Switches ("ada") use Safir_Defaults.Builder'Default_Switches ("Ada");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use Safir_Defaults.Compiler'Default_Switches ("Ada");
   end Compiler;

   package Linker is
      for Default_Switches ("ada") use Safir_Defaults.Linker'Default_Switches ("Ada");
   end Linker;

end Dose_Test_Ada;

