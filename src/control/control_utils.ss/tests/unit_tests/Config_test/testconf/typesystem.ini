dots_shared_memory_size=10

dou_search_path=

# The header ControlConfig.h (which is the target for this test) has a dependency to
# safir_generated-Core-cpp but since this dependency is explicitly stated in the corresponding
# CMakeLists.txt we don't need a Core section here
 
[TestBase]
kind=override
dou_directory=$(DOU_TEST_DIR)/../dou

[Test]
kind=override
dou_directory=$(DOU_TEST_DIR)

