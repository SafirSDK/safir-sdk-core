with "externally_built_library";

project dots_generated_ada is

    for Source_Dirs use (external ("SAFIR_SDK") & "/ada/dots_generated_ada/interface");

    for library_dir use Externally_Built_Library'library_dir;

    for Library_Name use "dots_generated_ada";

    for Library_ALI_Dir use external ("SAFIR_SDK") & "/ada/dots_generated_ada/ali";

    for Library_Kind use "dynamic";

    for Externally_Built use "true";

end dots_generated_ada;
