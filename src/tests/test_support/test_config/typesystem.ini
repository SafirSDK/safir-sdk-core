;Number of megabytes of shared memory that will be allocated by dots kernel.
dots_shared_memory_size=10

dou_search_path=

[Core]
kind=override
dou_directory=$(SAFIR_SOURCE_ROOT)/src/safir_dou

#This is not really an override, as such. But we only want to load the dou files, not tp attempt to load any libraries, since they should be linked in if needed.
