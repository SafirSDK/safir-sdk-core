dots_shared_memory_size=10

dou_search_path=

[Core]
kind=override
dou_directory=$(SAFIR_SOURCE_ROOT)/src/safir_dou

#This is not really an override, as such. But we only want to load the dou files, not
#attempt to load any libraries, since they should be linked in if needed.

[Override]
kind=override
dou_directory=$(SAFIR_SOURCE_ROOT)/src/tests/test_support/test_config/override
