; SAFIR_TEST_TEMP variable is set by the run_dose_tests.py script.
;
; On jenkins it will be %WORKSPACE%/temp.
; Otherwise it will be @{TEMP} which is resolved by lluf_config to /tmp on linux and %TEMP% or %TMP% on windows
;
lock_file_directory=$(SAFIR_TEST_TEMP)/safir-sdk-core/lock
crash_dump_directory=$(SAFIR_TEST_TEMP)/safir-sdk-core/crash_dumps
ipc_endpoints_directory=$(SAFIR_TEST_TEMP)/safir-sdk-core/ipc



