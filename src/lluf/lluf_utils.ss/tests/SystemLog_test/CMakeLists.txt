FILE(GLOB sources *.cpp)

ADD_EXECUTABLE(SystemLog_test ${sources})

TARGET_LINK_LIBRARIES(SystemLog_test PRIVATE lluf_utils)

ADD_TEST(NAME SystemLog
  COMMAND ${PYTHON_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/run_test.py ${CMAKE_CURRENT_SOURCE_DIR}/testconf)

SET_SAFIR_TEST_PROPERTIES(TEST SystemLog)
