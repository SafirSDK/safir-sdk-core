ADD_EXECUTABLE(run_sql run_sql.cpp)

TARGET_LINK_LIBRARIES(run_sql PRIVATE
  dots_cpp
  olib
  ${SAFIR_ODBC_LIBRARIES}
  ${Boost_PROGRAM_OPTIONS_LIBRARY}
)

SAFIR_INSTALL(TARGETS run_sql TEST_SUITE)

INSTALL(PROGRAMS run_dope_odbc_backend_test.py
  DESTINATION ${SAFIR_INSTALL_DESTINATION_BIN}
  COMPONENT Test)
