The FORCE_LOG functionality is only tested in the tracer_cpp tests, since it is all implemented in the library. No need to test it in the other languages.

The tracer backdoor is also only tested in c++, in the backdoor_tracer tests. Again this is library functionality.
