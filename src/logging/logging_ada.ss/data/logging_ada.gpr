with "externally_built_library";
with "logging_library";

project Logging_Ada is

    for Source_Dirs use (External ("SAFIR_SDK") & "/ada/logging_ada/interface");

    for Library_Dir use Externally_Built_Library'Library_Dir;

    for Library_Name use "logging_ada";

    for Library_ALI_Dir use External ("SAFIR_SDK") & "/ada/logging_ada/ali/";

    for Library_Kind use "dynamic";

    for Externally_Built use "true";

end Logging_Ada;
